# 📚 BookStore Telegram Bot

Телеграм-бот для продажи электронных книг с обработкой платежей через чеки.

## 🚀 Быстрый старт

### Локальная разработка

1. **Клонируйте репозиторий** (если используете Git)
   ```bash
   git clone <your-repo-url>
   cd BookStoreBot
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   
   Создайте файл `.env` в корне проекта:
   ```env
   BOT_TOKEN=your_bot_token_here
   ADMIN_ID=your_telegram_user_id
   CARD_NUMBER=2202 2063 4567 8901
   CARD_HOLDER=Иванов Иван Иванович
   ```
   
   Или экспортируйте переменные в терминале:
   ```bash
   export BOT_TOKEN="your_bot_token_here"
   export ADMIN_ID="your_telegram_user_id"
   ```

4. **Запустите бота**
   ```bash
   npm start
   ```

### Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Скопируйте токен в переменную окружения `BOT_TOKEN`

### Получение ID администратора

1. Откройте Telegram и найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Бот вернет ваш User ID
4. Скопируйте ID в переменную окружения `ADMIN_ID`

## 🚂 Деплой на Railway

Railway автоматически определяет Node.js проекты и использует команду `npm start` для запуска.

### Шаг 1: Подготовка репозитория

1. Инициализируйте Git репозиторий (если еще не сделано):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. Создайте репозиторий на GitHub и загрузите код:
   ```bash
   git remote add origin <your-github-repo-url>
   git push -u origin main
   ```

### Шаг 2: Деплой на Railway

1. Перейдите на [Railway](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий с ботом
6. Railway автоматически определит проект и начнет деплой

### Шаг 3: Настройка переменных окружения

1. В проекте Railway перейдите на вкладку **"Variables"**
2. Добавьте следующие переменные окружения:

   | Переменная | Описание | Пример |
   |------------|----------|--------|
   | `BOT_TOKEN` | Токен бота от @BotFather | `123456789:ABCdefGHIjklMNOpqrsTUVwxyz` |
   | `ADMIN_ID` | Telegram User ID администратора | `123456789` |
   | `CARD_NUMBER` | Номер карты для оплаты (опционально) | `2202 2063 4567 8901` |
   | `CARD_HOLDER` | Имя держателя карты (опционально) | `Иванов Иван Иванович` |

3. После добавления переменных Railway автоматически перезапустит приложение

### Шаг 4: Проверка работы

1. Откройте вкладку **"Deployments"** в Railway
2. Дождитесь успешного деплоя (зеленый статус)
3. Откройте Telegram и найдите вашего бота
4. Отправьте команду `/start`

## 📁 Структура проекта

```
BookStoreBot/
├── bot.js                 # Основной файл бота
├── package.json           # Зависимости и скрипты
├── .gitignore            # Игнорируемые файлы Git
├── README.md             # Документация
└── nodejs_bookstore_bot.js # Старый файл (можно удалить)
```

## 🔧 Настройка

### Изменение каталога книг

Отредактируйте массив `books` в файле `bot.js`:

```javascript
const books = [
  {
    id: 1,
    title: 'Название книги',
    author: 'Автор',
    price: 500,
    available: true,
    driveLinks: {
      pdf: 'https://drive.google.com/...',
      epub: 'https://drive.google.com/...'
    }
  },
  // ... другие книги
];
```

### Изменение реквизитов для оплаты

Используйте переменные окружения `CARD_NUMBER` и `CARD_HOLDER`, или измените значения по умолчанию в функции `showPaymentInfo()`.

## 📝 Команды бота

- `/start` - Начать работу с ботом, показать приветствие

## 🔒 Безопасность

- **Никогда не коммитьте** файл `.env` в Git
- Храните токен бота только в переменных окружения Railway
- Регулярно проверяйте логи на наличие ошибок

## 🐛 Решение проблем

### Бот не отвечает

1. Проверьте, что `BOT_TOKEN` установлен правильно
2. Проверьте логи в Railway (вкладка "Deployments" → "View Logs")
3. Убедитесь, что бот не заблокирован

### Ошибка "ADMIN_ID не найден"

1. Убедитесь, что переменная `ADMIN_ID` установлена в Railway
2. Проверьте, что ID указан как число (без кавычек)

### Ошибки при деплое на Railway

1. Проверьте, что `package.json` содержит правильные скрипты
2. Убедитесь, что указана версия Node.js в `engines`
3. Проверьте логи деплоя на наличие ошибок

## 📚 Дополнительные ресурсы

- [Документация node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)
- [Документация Railway](https://docs.railway.app)
- [Telegram Bot API](https://core.telegram.org/bots/api)

## 📄 Лицензия

ISC

